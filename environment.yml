name: brmda_py39
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.9
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - tqdm
  - openpyxl
  # Install PyTorch from the pytorch channel (CPU/GPU variant depends on your platform)
  - pytorch
